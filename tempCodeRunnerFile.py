filetypes = (
            #         ('text files', '*.txt'),
            #         ('All files', '*.*')
            #     ) 

            # # filename = fd.askopenfilename(
            # #     title='Open a file',
            # #     initialdir='/',
            # #     filetypes=filetypes)
            # filename = fd.askopenfilename()

            # showinfo(
            #     title='Selected File',
            #     message = filename
            # )
            
            # print(filename)